# Copy this file to .streamlit/secrets.toml for LOCAL development.
# NEVER commit the real secrets.toml to GitHub (it's already in .gitignore).
#
# On Streamlit Community Cloud: go to your app > Settings > Secrets
# and paste the same key = "value" lines there instead of uploading this file.
#
# Get a FREE Gemini API key (no billing required) at:
# https://aistudio.google.com/apikey

GEMINI_API_KEY = "AQ.Ab8RN6KtYKGJCO4ZRqwm1I-B-0xX1A8MWHpGHpdmw4YmsB8_8A"

# Optional: model override. Defaults to "gemini-flash-latest" (an alias that
# always points to Google's newest free-tier Flash model) if omitted.
GEMINI_MODEL = "gemini-flash-latest"
